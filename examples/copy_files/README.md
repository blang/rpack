# Copy files

Copies bundled files and user-mapped files/directories to the target. Demonstrates conditional logic with values, `rpack.copy`, `rpack.read_dir`, and the `filepath` module for path manipulation.

```shell
cd use && rpack run ./copy.rpack.yaml
```
