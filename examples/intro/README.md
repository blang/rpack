# Intro

Reads a user-mapped YAML file, templates a Markdown file with Go `text/template`, and copies a static file. Demonstrates `rpack.read`, `rpack.from_yaml`, `rpack.template`, `rpack.write`, and `rpack.copy`.

```shell
cd use && rpack run ./intro.rpack.yaml
```
